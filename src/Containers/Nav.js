import React from 'react';

const Nav = () => {
	return (
		<div>nav</div>
	)
}

const styles = {};

styles.nav = {
  padding: 0,
  margin: 0,
  position: "absolute",
  top: 0,
  height: "40px",
  width: "100%",
  display: "flex"
};

export default Nav;